apply plugin: 'java'

group 'com.avairebot'
version '1.0-SNAPSHOT'
description 'A super simple plugin that makes the bot stay connected to voice channels.'

sourceCompatibility = 1.8
targetCompatibility = 1.8

repositories {
    //
}

dependencies {
    // Due to jitpack not wanting to compile the sources from github,
    // we're using the raw Jar file instead.
    compileOnly files('./../avaire/AvaIre.jar')
}
